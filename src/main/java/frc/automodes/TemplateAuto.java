package frc.automodes;

import edu.wpi.first.wpilibj.command.CommandGroup;

public class TemplateAuto extends CommandGroup{

    public TemplateAuto() {
        /*
        Add logic for the automode using addParallel(command) and addSequential(command).

        addParallel excutes a command parallel to the next sequential command

        addSequential excutes a command after the previous sequential command is finished

        eg. Drive forward while raising the cascade, then turn 90 degrees

        addParallel(IncreaseCascade);
        addSequential(DriveForward);
        addSequential(Turn90);

        */
    }

}